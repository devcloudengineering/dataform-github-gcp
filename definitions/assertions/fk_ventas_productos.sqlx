config {
    type: "assertion",
    tags: ["assert_crit"],
    dependencies: ["fct_ventas", "dim_productos"]
}

SELECT
  v.*
FROM
  ${ref("fct_ventas")} v
LEFT JOIN
  ${ref("dim_productos")} p
USING
  (producto_id)
WHERE
  p.producto_id IS NULL
